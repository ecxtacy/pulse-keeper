import express from "express";

const getProfile = (req: express.Request, res: express.Response) => {};
const createProfile = (req: express.Request, res: express.Response) => {};
const updateProfile = (req: express.Request, res: express.Response) => {};

export const profileController = { getProfile, createProfile, updateProfile };
