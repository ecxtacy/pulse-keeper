{
  "name": "@pulse-keeper/server",
  "version": "1.0.0",
  "description": "Backend expressjs server for pulse-keeper.",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "nodemon",
    "build": "tsc -b",
    "start": "node dist/server.js",
    "lint": "eslint . --ext ts",
    "lint:fix": "eslint . --fix --ext ts",
    "format": "prettier --write src/*",
    "db:docker": "bash ../tools/run_postgres_docker.sh"
  },
  "author": "ecxtacy",
  "license": "Apache-2.0"
}
